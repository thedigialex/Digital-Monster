version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: digi_portal
    volumes:
      - .:/var/www
    networks:
      - laravel

volumes:
  dbdata:

networks:
  laravel:
    driver: bridge
